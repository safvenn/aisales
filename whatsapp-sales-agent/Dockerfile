# This Dockerfile is for the backend service.
# The root compose orchestrates all services via docker-compose.yml
# See backend/Dockerfile for the Node.js API image.
FROM docker.n8n.io/n8nio/n8n:latest
